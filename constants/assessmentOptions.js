/**
 * Assessment Options Configuration
 *
 * Modify these values to change assessment options without rebuilding the app.
 * Changes will be reflected immediately when fetched from the API.
 */

module.exports = ["Satisfactory", "Needs Improvement", "Unsatisfactory"];
